server:
  port: 8082
spring:
  application:
    name: audit-service
  datasource:
    url: jdbc:h2:mem:audit;MODE=Oracle;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
management.endpoints.web.exposure.include: health,info
